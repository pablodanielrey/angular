angular
  .module('mainApp')
  .controller('accessCtrl', AccessCtrl);

AccessCtrl.$inject = ['$rootScope','$scope', '$wamp', '$window'];

function AccessCtrl($rootScope, $scope, $wamp, $window) {

};
